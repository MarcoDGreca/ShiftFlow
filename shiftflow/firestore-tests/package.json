{
  "name": "shiftflow-firestore-tests",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Suite di test sulle regole di sicurezza Firestore (RNF2), su emulatore.",
  "scripts": {
    "test": "firebase emulators:exec --only firestore --config ../firebase.json --project demo-shiftflow 'node --test'"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.1",
    "firebase": "^12.16.0"
  }
}
